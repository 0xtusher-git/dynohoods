# Copy to .env.local and fill in. Without these, submissions go to
# an in-memory store (fine for local dev, resets on restart).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: overwrite to change the daily submission limit per IP.
# SUBMISSION_RATE_LIMIT_PER_IP=5
